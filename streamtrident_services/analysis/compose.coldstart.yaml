# Worker override: cold_start mode. Use after compose.yaml and optional environment overrides.
services:
  trident-worker:
    command: ["python", "-m", "app.worker", "--config", "config/trident.yaml", "--mode", "cold_start"]
