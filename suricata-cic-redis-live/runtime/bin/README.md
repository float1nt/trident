# Suricata binary (not in git)

The modified Suricata executable exceeds GitHub's file size limit.

After building Suricata locally, copy it here:

```bash
./scripts/sync_runtime.sh
./scripts/build_image.sh
```

Expected path: `runtime/bin/suricata`
